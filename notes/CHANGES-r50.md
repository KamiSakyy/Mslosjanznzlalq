# KamiGram 12.10.3 — обновление R50 (крупное, все 15 пунктов)

Версия: `mod-12.10.3-mod-r52` (CI run **35732983146**, коммит `8eca5b5`, база — `DrKLO/Telegram` @ `9552e554`).
Исходник предыдущей версии (r49) **не удалён** — он остаётся в handoff.
Ветка: `arena/01a0bfd6-mslosjanznzlalq`.

Тег релиза: `mod-12.10.3-mod-r52` (Latest). APK: `handoff/apk/KamiGram-12.10.3-mod-r52.apk`
(35 718 214 байт, sha256 `6c2705442a19fa717efc48e892665f2ba65a9a4e102bfa1570dda79ed322105b`).

> Сборки r50/r51 не выпускались: компилятор поймал ошибки типов (имя локальной переменной
> конфликтовало с полем диалога; `AboutLinkCell.setTextAndValue` требует `String`).
> Исправлено в r52, релиз только один.

## Что сделано по пунктам запроса

| № | Запрос | Как сделано | Где в коде |
|---|--------|-------------|------------|
| 1 | Прокси должны работать при включённом VPN | Убрана «пауза из-за VPN» в маршрутизаторе: VPN больше не отключает KamiProxy | `KamiGramBuiltinProxy.route()` / `statusText()` |
| 2 | Имя «KamiGram» исчезает при включённом прокси | Родной оверлей Telegram больше не подменяет заголовок: имя остаётся, статус связи/прокси выводится рядом (мелким приглушённым текстом) | `KamiGramProxyStatus` + `DialogsActivity` + `LaunchActivity.updateCurrentConnectionState` |
| 3 | Красные кружки непрочитанных | Из палитры темы убраны переопределения `chats_unreadCounter` / `chats_unreadCounterMuted` — цвета родные Telegram (проверено: `night.attheme` = сток) | `apply_theme_pro.py` |
| 4 | Убрать подсказки и «детские» надписи | Переписаны тексты центра: короче и по делу, «мощное» и прочие хвастливые подписи убраны | `KamiGramCenter`, `KamiGramProxyPower` |
| 5 | Окно KamiGram перегружено, кнопка «Сохранить» далеко | Центр переделан: 5 разделов (Трафик, Приватность, Прокси, Вид, Прочее) — переключение одним касанием, внутри короткие списки; настройки применяются сразу; снизу **закреплённая** кнопка «Готово» (листать не надо) | `KamiGramCenter` (переписан) |
| 6 | Оптимизация и плавность без потери функций | Новый класс: поднимает класс производительности, включает анимации/размытие по настройке; ничего не отключает навсегда | `KamiGramOptimize` + переключатели в центре |
| 7 | В «Избранном» только три точки | В режиме «Избранное» иконка-скрепка (`mini_attach`) не создаётся | `ChatActivity` (`KAMIGRAM_SAVED_NO_ATTACH`) |
| 8 | ID между описанием и ссылкой @username | ID дописывается прямо под описанием (профиль, канал, группа, бот), выше строки @username; из меню «три точки» ID убран | `KamiGramIds.aboutWithId()` + `ProfileActivity` (3 точки привязки) |
| 9 | Призрак + отложка: «message id нету» | Убрана «тихая отправка» через подмену сообщения на отложенное (+5 с) — именно она ломала отложенные и запланированные сообщения. Отправка снова обычная | `apply_r43_patches.py`: `ghost_silent_send()` больше не вызывается |
| 10 | Призрак по умолчанию выключен | `KamiGramConfig.defaultValue()`: `KEY_GHOST` и `KEY_GHOST_SEND` → `false` | `KamiGramConfig` |
| 11 | Иконка призрака РЯДОМ с тремя точками | Пункт меню с иконкой призрака создаётся **перед** пунктом «⋮» (значит — рядом с ним); касание включает/выключает, состояние видно (акцент + яркость иконки) | `KamiGramGhost.addHeaderItem/bindHeader/refreshHeader` + `ChatActivity` (`KAMIGRAM_GHOST_ICON`) |
| 12 | Честный онлайн | При обычной (не отложенной) отправке приложение **реально** выходит в сеть на секунду (`account.updateStatus offline=false`, затем `offline=true`). Поддельная строка «был в сети» из профиля убрана | `KamiGramGhost.onRealSend()` + `SendMessagesHelper.sendMessage()` + `KamiGramNetFilter` |
| 13 | Свой шрифт .ttf из проводника Google | Выбор файла через системный SAF-проводник, копия во внутреннюю папку, подмена шрифта в двух родных точках Telegram + сброс кэша и перерисовка; кнопка «Сбросить шрифт» | `KamiGramFont` + `AndroidUtilities.getTypeface()/bold()` + `LaunchActivity.onActivityResult` |
| 14 | Дизайн лучше и качественнее | Новый центр (карточки 16 dp, разделители с отступом, приглушённые подписи, акцентные действия), аккуратная строка состояния прокси в шапке | `KamiGramCenter`, `KamiGramProxyStatus` |
| 15 | Стикеры и премиум-эмодзи вернуть, по умолчанию включены | `NO_STICKERS=0` в CI (блокировка стикеров на уровне `MediaDataController` больше не ставится), в сетевом фильтре стикеры/эмодзи отсекаются только своим переключателем; дефолты `KEY_NO_STICKERS`/`KEY_NO_ANIMATED_EMOJI` → `false` | `.github/workflows/build-tgmod.yml`, `KamiGramNetFilter`, `KamiGramConfig` |

## Новые файлы

* `mod/kamigram/KamiGramFont.java` — свой шрифт приложения (.ttf/.otf через SAF, сброс).
* `mod/kamigram/KamiGramOptimize.java` — оптимизация и плавность.
* `mod/kamigram/KamiGramProxyStatus.java` — название приложения + статус прокси в шапке.
* `mod/kamigram/apply_r50_patches.py` — патч-пакет R50 (14 правок, идемпотентный, отчёт `MOD_R50_FEATURES.txt`).
* `mod/kamigram/res/drawable/kamigram_ghost.xml` — векторная иконка призрака (контурная, в цвет пунктов меню).

## Проверка перед сборкой

* Пайплайн `mod/apply-mod.sh` прогнан локально на чистом клоне `DrKLO/Telegram` @ `9552e554`:
  `exit=0`, все пакеты P11–P97 применились, `P97 ПРАВКИ R50: применено пунктов — 14 (отчёт: MOD_R50_FEATURES.txt)`.
* Синтаксис всех Java-классов мода проверен парсером Java.
* Палитра темы сверена со стоковой: `chats_unreadCounter` / `chats_unreadCounterMuted` больше не переопределяются (красных кружков нет).

## Что осталось на будущее (не забыто)

* Тексты «хвостов» старых пакетов (P25/P26) всё ещё упоминают VPN/прямое соединение — поведение исправлено (прокси работают при VPN), формулировки в отчётах сборки косметические.
* `KamiGramNetFilter` скрывает витрину Premium/Stars/TON, пока пользователь не включит её сам (переключатель «Без Premium, Stars и TON» в центре).
