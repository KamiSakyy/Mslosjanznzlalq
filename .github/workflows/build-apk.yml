name: Build APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master, arena/* ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.7

      - name: Accept Android SDK licenses
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses || true

      - name: Install required SDK packages
        run: |
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Generate Gradle wrapper
        run: |
          gradle wrapper --gradle-version 8.7
          chmod +x gradlew

      - name: Build debug APK
        id: build
        run: |
          ./gradlew assembleDebug --stacktrace --no-daemon 2>&1 | tee build.log
          echo "=== TAIL ==="
          tail -n 300 build.log
          echo "=== LIST BUILD ==="
          ls -lhR app/build/ 2>&1 | head -n 500
          echo "=== FIND APK ==="
          find app -name "*.apk" -type f -ls 2>&1 || echo "No APK"

      - name: Copy APK to handoff
        if: always()
        run: |
          mkdir -p handoff
          cp build.log handoff/build.log || true
          find app/build -name "*.apk" -type f -exec cp -v {} handoff/ \; || true
          ls -lh handoff/ || true
          if [ -f handoff/app-debug.apk ]; then
            cp handoff/app-debug.apk handoff/lumi-aether-debug.apk || true
          else
            APK=$(find handoff -name "*.apk" | head -n 1)
            if [ ! -z "$APK" ]; then
              cp "$APK" handoff/app-debug.apk
              cp "$APK" handoff/lumi-aether-debug.apk
            fi
          fi
          echo "Built at $(date) commit $GITHUB_SHA" > handoff/BUILD_INFO.txt
          ls -lh handoff/*.apk 2>&1 >> handoff/BUILD_INFO.txt || echo "No APK in handoff" >> handoff/BUILD_INFO.txt
          cat handoff/BUILD_INFO.txt

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: |
            handoff/
            app/build/outputs/
            build.log
          if-no-files-found: warn

      - name: Commit handoff
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add handoff/ || true
          if git diff --staged --quiet; then
            echo "No changes"
          else
            git commit -m "ci: update handoff with APK and logs [skip ci]"
            git push origin HEAD:${{ github.ref_name }}
          fi
